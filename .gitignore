# env (never commit keys)
.env
.env.local
.env.*.local

# local data — reality lives here, not in git
*.duckdb
*.duckdb.wal
data/raw/
pipeline-checks/

# local-only claude config
.claude/settings.local.json

# machine-local spawn doc, not part of the submission
SPAWN.md

# logs / editor / OS
logs
*.log
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
__pycache__/
.venv/

# pilot runtime artifacts (binaries, object store data, caches, derived exports)
pilot/bin/
pilot/minio-data/
pilot/consolidated/
pilot/state/
pilot/query-table-export/
pilot/*.zip
pilot/loader.env
pilot/zips/
pilot/production-rows.json

# playwright screenshots (local test artifacts)
.playwright-mcp/

# deploy artifacts
deploy/parquet-host/*.parquet
deploy/mcp/node_modules/

# netlify CLI local link state
.netlify/
ui/.netlify/
